## Summary

- what changed
- why it changed

## Checklist

- [ ] tests added/updated for behavior changes
- [ ] `go test ./...` passes locally
- [ ] docs/readme updated if user-visible behavior changed
- [ ] generated files regenerated if upstream/spec changed
- [ ] compatibility check passes (`./scripts/check-compat.sh`)

## Validation

Commands run:

```bash
# paste commands and short outputs
```
